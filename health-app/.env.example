# API base URL (backend). No trailing slash. Used by src/config.ts.
# Local dev: http://localhost:8000/api when backend runs on port 8000 (routes are under /api).
# Production: set at build time (e.g. Docker) instead of committing here.
VITE_API_URL=http://localhost:8000/api
