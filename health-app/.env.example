# Backend API base URL (no trailing slash). For LOCAL DEV only.
# Use http://localhost:8000 when running backend locally (npm run dev).
# For Cloud Run: do NOT put the production URL here. Set VITE_API_URL at Docker build time instead.
VITE_API_URL=http://localhost:8000
