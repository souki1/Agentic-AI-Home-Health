# ============================================================================
# ENVIRONMENT FILES TEMPLATE
# ============================================================================
# 
# This is a template file. Copy it to create your environment files:
#
# For LOCAL DEVELOPMENT:
#   copy .env.example .env.local
#   (or create .env.local manually)
#
# For PRODUCTION REFERENCE:
#   See .env.production (committed to git as reference)
#   Note: In Cloud Run, env vars are set via Cloud Run, not .env files
#
# ============================================================================
# LOCAL DEVELOPMENT (.env.local)
# ============================================================================

# Database: local PostgreSQL
DATABASE_URL=postgresql://postgres:root@localhost:5432/health_analytics

# Auth
SECRET_KEY=root

# CORS: local frontend URLs
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000

# ============================================================================
# PRODUCTION (GCP Cloud Run) - Set via Cloud Run env vars/secrets, NOT .env
# ============================================================================
# See .env.production for production reference values
# 
# For Cloud Run, set these in Cloud Run service (not in .env):
# 
# Secrets (via Secret Manager):
#   DATABASE_URL=postgresql://... (if using direct connection)
#   SECRET_KEY=your-production-secret
#   DB_PASS=your-db-password (if using Cloud SQL Unix socket)
#
# Environment variables:
#   DATABASE_URL=postgresql://healtappdb:PASSWORD@34.171.27.98:5432/health_analytics?sslmode=require (or from secret)
#   INSTANCE_CONNECTION_NAME=tech-83b89:us-central1:healtappdb (if using socket)
#   DB_USER=health_user
#   DB_NAME=health_analytics
#   CORS_ORIGINS=https://agentic-ai-home-health-79306395653.europe-west1.run.app
#   GOOGLE_CLOUD_PROJECT=tech-83b89
#   VECTOR_SEARCH_INDEX_ENDPOINT_ID=tech-83b89-v1
#   GOOGLE_CLOUD_LOCATION=us-central1
#
# See: backend/docs/ENV-VARIABLES.md for complete list

# ----- GCP Vertex AI RAG (optional) -----
# No API key: Vertex AI (including Vector Search) uses project + Application Default
# Credentials (gcloud auth application-default login) or GOOGLE_APPLICATION_CREDENTIALS.
# GOOGLE_CLOUD_PROJECT=your-gcp-project
# GOOGLE_CLOUD_LOCATION=us-central1
# Endpoint ID of your deployed Vector Search index (from Vertex AI console), not an API key
# VECTOR_SEARCH_INDEX_ENDPOINT_ID=your-endpoint-id
# VECTOR_SEARCH_INDEX_ID=your-index-id
# VERTEX_EMBEDDING_MODEL=text-embedding-005
# VERTEX_EMBEDDING_DIMENSIONS=768
# VERTEX_RAG_LLM_MODEL=gemini-1.5-flash-001
# VECTOR_SEARCH_TOP_K=10
# RAG_CHUNK_SIZE=512
# RAG_CHUNK_OVERLAP=50
# Path to JSON file with chunk id -> text for lookup (object or [{"id","text"}])
# RAG_CHUNK_STORE_PATH=./rag_chunks.json
