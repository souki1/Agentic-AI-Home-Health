# Production defaults. Cloud Run sets DATABASE_URL and SECRET_KEY via Secrets.
# For local prod-like run, set DATABASE_URL and SECRET_KEY in env or another .env.
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

HOST=0.0.0.0
PORT=8080
CORS_ORIGINS=https://agentic-ai-home-health-79306395653.europe-west1.run.app

# LLM / RAG (cloud uses Vertex AI) - required for chat on Cloud Run
LLM_PROVIDER=vertex
VERTEX_MODEL=gemini-1.5-flash
GOOGLE_CLOUD_PROJECT=tech-83b89
GOOGLE_CLOUD_LOCATION=us-central1
