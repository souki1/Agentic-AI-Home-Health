# Production defaults (Cloud Run overrides DATABASE_URL and SECRET_KEY via Secrets)
# Used when running locally with production-like config; Cloud Run uses service env/secrets.
PORT=8080
HOST=0.0.0.0
CORS_ORIGINS=https://agentic-ai-home-health-79306395653.europe-west1.run.app
