# ============================================================================
# PRODUCTION ENVIRONMENT (.env.production)
# Reference only. In Cloud Run, values come from env vars/secrets set at deploy.
# ============================================================================

# Database: Backend prefers DATABASE_URL (TCP). Set in Secret Manager as DATABASE_URL_SECRET.
# Use URL-encoding for password: @ -> %40 (e.g. Girish@123 -> Girish%40123)
DATABASE_URL=postgresql://healtappdb:Girish%40123@34.171.27.98:5432/health_analytics?sslmode=require

# If not using DATABASE_URL, socket connection uses these (set by cloudbuild):
INSTANCE_CONNECTION_NAME=tech-83b89:us-central1:healtappdb
DB_USER=healthappdb
DB_NAME=health_analytics
DB_PORT=5432
DB_SSLMODE=require

# Auth (set via SECRET_KEY_SECRET in Cloud Run)
SECRET_KEY=your-production-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# CORS: Production frontend URL
CORS_ORIGINS=https://agentic-ai-home-health-79306395653.europe-west1.run.app,http://localhost:5173

# ============================================================================
# GCP Vertex AI RAG (optional)
# ============================================================================
GOOGLE_CLOUD_PROJECT=tech-83b89
GOOGLE_CLOUD_LOCATION=us-central1
VECTOR_SEARCH_INDEX_ENDPOINT_ID=tech-83b89-v1
# VECTOR_SEARCH_INDEX_ID=your-index-id
# VERTEX_EMBEDDING_MODEL=text-embedding-005
# VERTEX_EMBEDDING_DIMENSIONS=768
# VERTEX_RAG_LLM_MODEL=gemini-1.5-flash-001
# VECTOR_SEARCH_TOP_K=10
# RAG_CHUNK_SIZE=512
# RAG_CHUNK_OVERLAP=50
