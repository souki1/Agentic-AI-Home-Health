# ============================================================================
# PRODUCTION ENVIRONMENT (.env.production)
# Reference file for production settings
# Note: In Cloud Run, these are set via environment variables/secrets, not .env files
# ============================================================================

# Database: Cloud SQL via Unix socket (recommended for Cloud Run)
# ALL values MUST be set - no hardcoded defaults in config.py
INSTANCE_CONNECTION_NAME=tech-83b89:us-central1:healtappdb
DB_USER=health_user
DB_NAME=health_analytics
DB_HOST=34.61.23.15
DB_PORT=5432
DB_PASS=root
DB_SSLMODE=require
# Alternative: Direct connection (if not using Cloud SQL Unix socket)
# DATABASE_URL=postgresql://user:password@host:5432/health_analytics?sslmode=require

# Auth (all required - set via Secret Manager in Cloud Run)
SECRET_KEY=your-production-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# CORS: Production frontend URL (required)
CORS_ORIGINS=https://agentic-ai-home-health-79306395653.europe-west1.run.app,http://localhost:5173

# ============================================================================
# GCP Vertex AI RAG (optional)
# ============================================================================
GOOGLE_CLOUD_PROJECT=tech-83b89
GOOGLE_CLOUD_LOCATION=us-central1
VECTOR_SEARCH_INDEX_ENDPOINT_ID=tech-83b89-v1
# VECTOR_SEARCH_INDEX_ID=your-index-id
# VERTEX_EMBEDDING_MODEL=text-embedding-005
# VERTEX_EMBEDDING_DIMENSIONS=768
# VERTEX_RAG_LLM_MODEL=gemini-1.5-flash-001
# VECTOR_SEARCH_TOP_K=10
# RAG_CHUNK_SIZE=512
# RAG_CHUNK_OVERLAP=50
